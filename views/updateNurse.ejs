<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Recptionist Dashboard</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .sidebar {
            width: 250px;
            background-color: #343a40;
            color: white;
            position: fixed;
            height: 100vh;
            padding: 15px;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
        }

        .sidebar li {
            padding: 10px;
            cursor: pointer;
        }

        .sidebar li:hover {
            background-color: #495057;
        }

        .content {
            margin-left: 270px;
            padding: 20px;
        }

        .collapsed {
            margin-left: 0 !important;
        }

        .toggle-btn {
            position: fixed;
            left: 260px;
            top: 10px;
            z-index: 1000;
        }

        .dark-mode {
            background-color: #1e1e1e;
            color: white;
        }

        .dark-mode .sidebar {
            background-color: #222;
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
     <%- include("partials/receptionSidebar")%>

    <!-- Main Content -->
    <div class="content" id="content">
        <h1>Welcome to the Receptionist Dashboard</h1>
        <p>Here you can manage patients, staff, appointments, and rooms.</p>
          <div class="container mt-5">
    <h2 class="mb-4">Register Nurse</h2>
    <form action="/api/receptionist/updatenurse" method="POST">
        <input type="hidden" name="nurseId" value="<%=nurse[0].nurseId%>">
      <!-- Name -->
      <div class="form-group">
        <label for="nurseName">Full Name</label>
        <input type="text" class="form-control" id="nurseName" name="name" value="<%=nurse[0].name%>" required>
      </div>

      <!-- Contact -->
      <div class="form-group">
        <label for="nurseContact">Contact</label>
        <input type="tel" class="form-control" id="nurseContact" name="contact" value="<%=nurse[0].contact%>" required>
      </div>

      <!-- Shift -->
      <div class="form-group">
        <label for="nurseShift">Shift</label>
        <select class="form-control" id="nurseShift" name="shift" value="<%=nurse[0].shift%>" required>
          <option value="">Select Shift</option>
          <option value="day">Day</option>
          <option value="night">Night</option>
        </select>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="btn btn-primary">Register Nurse</button>
    </form>
  </div>
   

    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleSidebar() {
            document.getElementById("sidebar").classList.toggle("collapsed");
            document.getElementById("content").classList.toggle("collapsed");
        }

        function toggleDarkMode() {
            document.body.classList.toggle("dark-mode");
        }
    </script>
</body>

</html>
